{
  "id": "java-binary-search-trees",
  "moduleId": "trees",
  "title": "Binary Search Trees",
  "description": "Adding an ordering rule to Binary Trees -- left is smaller, right is bigger -- enabling O(log n) search, echoing Binary Search's halving idea.",
  "objectives": [
    "Understand the BST ordering property",
    "Implement insert and search recursively",
    "Recognize why BST operations are O(log n) for a balanced tree, but O(n) for a skewed one"
  ],
  "sections": [
    {
      "type": "heading",
      "text": "1. The Rule: Left Smaller, Right Bigger"
    },
    {
      "type": "paragraph",
      "text": "A plain Binary Tree makes no promises about ordering. A Binary Search Tree (BST) adds exactly one rule, applied at EVERY node: everything in the left subtree is smaller, and everything in the right subtree is bigger."
    },
    {
      "type": "definition",
      "term": "Binary Search Tree",
      "text": "A binary tree where every node's left subtree contains only smaller values and its right subtree contains only larger values, enabling efficient searching."
    },
    {
      "type": "heading",
      "text": "2. Inserting Into a BST"
    },
    {
      "type": "code",
      "lang": "java",
      "filename": "Main.java",
      "code": "public class Main {\n    static class Node {\n        int data;\n        Node left, right;\n        Node(int data) { this.data = data; }\n    }\n\n    static Node insert(Node node, int value) {\n        if (node == null) {\n            return new Node(value); // base case -- found the empty spot\n        }\n        if (value < node.data) {\n            node.left = insert(node.left, value);\n        } else if (value > node.data) {\n            node.right = insert(node.right, value);\n        }\n        return node;\n    }\n\n    static void printInOrder(Node node) {\n        if (node == null) return;\n        printInOrder(node.left);\n        System.out.print(node.data + \" \");\n        printInOrder(node.right);\n    }\n\n    public static void main(String[] args) {\n        Node root = null;\n        int[] values = {50, 30, 70, 20, 40, 60, 80};\n        for (int v : values) {\n            root = insert(root, v);\n        }\n        printInOrder(root);\n    }\n}",
      "output": "20 30 40 50 60 70 80 ",
      "retypePractice": true,
      "retypeId": "java-bst-insert"
    },
    {
      "type": "dryRun",
      "title": "Tracing insert(root, 40) where root already has 50 (left:30) inserted",
      "steps": [
        "insert(node=50, 40): 40 < 50 -- recurse into node.left (30)",
        "insert(node=30, 40): 40 > 30 -- recurse into node.right (currently null)",
        "insert(node=null, 40): base case -- return new Node(40)",
        "Back at node=30: node.right = new Node(40)",
        "Back at node=50: node.left stays 30 (unchanged reference), just its right child updated"
      ]
    },
    {
      "type": "note",
      "text": "This is exactly the recursive pattern from Recursion Basics and the height calculation in Binary Trees: a base case (null -- create the node) and a recursive case that moves toward it, based on comparing value against the current node."
    },
    {
      "type": "heading",
      "text": "3. Searching a BST"
    },
    {
      "type": "code",
      "lang": "java",
      "filename": "Main.java",
      "code": "public class Main {\n    static class Node {\n        int data;\n        Node left, right;\n        Node(int data) { this.data = data; }\n    }\n\n    static Node insert(Node node, int value) {\n        if (node == null) return new Node(value);\n        if (value < node.data) node.left = insert(node.left, value);\n        else if (value > node.data) node.right = insert(node.right, value);\n        return node;\n    }\n\n    static boolean search(Node node, int value) {\n        if (node == null) {\n            return false; // reached an empty spot -- not found\n        }\n        if (value == node.data) {\n            return true;\n        }\n        return value < node.data ? search(node.left, value) : search(node.right, value);\n    }\n\n    public static void main(String[] args) {\n        Node root = null;\n        int[] values = {50, 30, 70, 20, 40, 60, 80};\n        for (int v : values) {\n            root = insert(root, v);\n        }\n\n        System.out.println(search(root, 60));\n        System.out.println(search(root, 100));\n    }\n}",
      "output": "true\nfalse",
      "retypePractice": true,
      "retypeId": "java-bst-search"
    },
    {
      "type": "warning",
      "text": "search(root, 100) MUST check every level down to a null before concluding 'not found' -- there's no shortcut for a value that genuinely isn't there. This mirrors Binary Search's own miss case, from the Binary Search lesson."
    },
    {
      "type": "heading",
      "text": "4. Why BST Search Echoes Binary Search"
    },
    {
      "type": "keyPoints",
      "title": "The parallel to Binary Search",
      "points": [
        "At each node, comparing value against node.data eliminates an ENTIRE subtree from consideration -- exactly like Binary Search eliminating half an array with one comparison",
        "For a BALANCED BST (roughly equal-sized left/right subtrees), this gives O(log n) search and insert, matching Binary Search's O(log n)",
        "printInOrder visits nodes in SORTED order automatically -- this connects directly to the next lesson, Tree Traversals, where in-order is formally covered"
      ]
    },
    {
      "type": "heading",
      "text": "5. The Catch: Skewed Trees"
    },
    {
      "type": "code",
      "lang": "java",
      "code": "// Inserting already-sorted values creates a skewed BST:\n// insert(1), insert(2), insert(3), insert(4), insert(5)\n// Result: 1 -> right -> 2 -> right -> 3 -> right -> 4 -> right -> 5\n// This is really just a LINKED LIST in disguise!"
    },
    {
      "type": "warning",
      "text": "If values are inserted in already-sorted order, EVERY node only ever gets a right child -- the tree degenerates into something structurally identical to a singly linked list (from the Singly Linked List lesson), and search/insert become O(n), not O(log n). Keeping a BST balanced (a topic beyond this course's current scope, handled by self-balancing trees like AVL or Red-Black trees) is what guarantees the O(log n) promise actually holds."
    },
    {
      "type": "commonMistakes",
      "title": "Common Beginner Mistakes",
      "items": [
        "Assuming ALL binary trees have BST ordering -- only a tree explicitly built and maintained with the BST insertion rule guarantees this property.",
        "Forgetting BST operations degrade to O(n) on a skewed (e.g. already-sorted-input) tree, since it's structurally just a linked list.",
        "Inserting a duplicate value without deciding a consistent rule for it (this example silently ignores duplicates via the else if) -- real implementations must pick and document a clear policy.",
        "Confusing search's O(log n) BEST case (balanced tree) with its O(n) WORST case (skewed tree) -- both are true depending on the tree's actual shape."
      ]
    },
    {
      "type": "interviewQuestions",
      "title": "Interview Questions",
      "questions": [
        {
          "question": "What is the defining property of a Binary Search Tree?",
          "answer": "For every node, all values in its left subtree are smaller, and all values in its right subtree are larger -- this ordering holds recursively at every node in the tree, not just the root."
        },
        {
          "question": "Why is BST search O(log n) on a balanced tree but O(n) on a skewed one?",
          "answer": "On a balanced tree, each comparison eliminates roughly half the remaining nodes, giving logarithmic depth to search. A skewed tree (e.g. built by inserting already-sorted values) degenerates into a structure equivalent to a linked list, where search must potentially check every node."
        },
        {
          "question": "What traversal of a BST produces values in sorted order?",
          "answer": "An in-order traversal (left subtree, then current node, then right subtree) -- covered fully in the Tree Traversals lesson -- visits every node's value in ascending sorted order, because of the BST property."
        }
      ],
      "thirtySecondAnswer": "A Binary Search Tree adds one rule to a plain binary tree: every node's left subtree holds only smaller values, and its right subtree holds only larger values. This lets insert and search compare the target against the current node and eliminate an entire subtree per step, mirroring Binary Search's halving -- giving O(log n) on a balanced tree. But if values are inserted in already-sorted order, the tree degenerates into a linked-list-like shape, and both operations become O(n). An in-order traversal of a BST always visits values in ascending sorted order, a direct consequence of the ordering property."
    },
    {
      "type": "memoryTrick",
      "title": "Trick to Remember",
      "text": "BST rule: smaller goes LEFT, bigger goes RIGHT, at every single node -- like a filing system that always halves your search. Feed it already-sorted data and it collapses into a lazy, lopsided linked list."
    },
    {
      "type": "quickCheck",
      "title": "Quick Understanding Check",
      "questions": [
        "What is the ordering property that defines a BST?",
        "Why is BST search O(log n) on a balanced tree but O(n) on a skewed one?",
        "What causes a BST to become skewed?",
        "Which traversal visits a BST's values in sorted order?"
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-700",
      "number": 700,
      "title": "Search in a Binary Search Tree",
      "difficulty": "Easy",
      "link": "https://leetcode.com/problems/search-in-a-binary-search-tree/",
      "problemStatement": "Given the root of a BST and an integer val, find the node whose value equals val and return the subtree rooted there. Return null if not found.",
      "examples": [
        { "input": "root = [4,2,7,1,3], val = 2", "output": "[2,1,3]" }
      ],
      "constraints": ["1 <= number of nodes <= 5000", "1 <= Node.val <= 10^7"],
      "hints": ["Use the BST ordering property to go left or right instead of searching both sides."],
      "approaches": [
        {
          "label": "Optimal",
          "explanation": "At each node, compare val: equal -> found; smaller -> go left; larger -> go right. Iterative, no backtracking needed since BST guarantees the target lives on exactly one path.",
          "code": "class Solution {\n    public TreeNode searchBST(TreeNode root, int val) {\n        while (root != null && root.val != val) {\n            root = val < root.val ? root.left : root.right;\n        }\n        return root;\n    }\n}",
          "output": "[2,1,3]",
          "timeComplexity": "O(h)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-701",
      "number": 701,
      "title": "Insert into a Binary Search Tree",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/insert-into-a-binary-search-tree/",
      "problemStatement": "Given the root of a BST and a value to insert, insert the value into the BST and return the root of the resulting tree (any valid insertion position is acceptable).",
      "examples": [
        { "input": "root = [4,2,7,1,3], val = 5", "output": "[4,2,7,1,3,5]", "explanation": "5 is inserted as the left child of 7 (5 < 7, 5 > 4)." }
      ],
      "constraints": ["0 <= number of nodes <= 10^4", "-10^8 <= Node.val <= 10^8"],
      "hints": ["Walk down using BST comparisons until you hit a null spot, then attach a new node there."],
      "approaches": [
        {
          "label": "Optimal",
          "explanation": "Recursively descend left or right based on comparison; when a null child slot is reached, create the new node there.",
          "code": "class Solution {\n    public TreeNode insertIntoBST(TreeNode root, int val) {\n        if (root == null) return new TreeNode(val);\n        if (val < root.val) {\n            root.left = insertIntoBST(root.left, val);\n        } else {\n            root.right = insertIntoBST(root.right, val);\n        }\n        return root;\n    }\n}",
          "output": "[4,2,7,1,3,5]",
          "timeComplexity": "O(h)",
          "spaceComplexity": "O(h)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-98",
      "number": 98,
      "title": "Validate Binary Search Tree",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/validate-binary-search-tree/",
      "problemStatement": "Given the root of a binary tree, determine if it is a valid BST (every node's value strictly between the min/max bounds inherited from its ancestors).",
      "examples": [
        { "input": "root = [5,1,4,null,null,3,6]", "output": "false", "explanation": "Node 4 is the right child of root 5, so every value in that subtree must be > 5. But 4 itself is only 4, which is less than 5 -- violates the BST property even though 4's own children (3 and 6) look locally fine." }
      ],
      "constraints": ["1 <= number of nodes <= 10^4", "-2^31 <= Node.val <= 2^31 - 1"],
      "hints": ["Checking only immediate parent-child order is NOT enough -- a node deep in the left subtree could still violate an ancestor far above it.", "Pass down a valid (min, max) range that narrows as you descend."],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "For every node, do a full inorder collection of its subtree and check local left < node < right relations only -- this misses violations against distant ancestors, so it is actually incorrect on trees like [5,1,4,null,null,3,6]. A truly correct brute force instead does a full inorder traversal of the WHOLE tree and checks the resulting list is strictly increasing.",
          "code": "class Solution {\n    public boolean isValidBST(TreeNode root) {\n        List<Integer> values = new ArrayList<>();\n        inorder(root, values);\n        for (int i = 1; i < values.size(); i++) {\n            if (values.get(i) <= values.get(i - 1)) return false;\n        }\n        return true;\n    }\n\n    private void inorder(TreeNode node, List<Integer> values) {\n        if (node == null) return;\n        inorder(node.left, values);\n        values.add(node.val);\n        inorder(node.right, values);\n    }\n}",
          "output": "false",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(n) for the collected list"
        },
        {
          "label": "Optimal",
          "explanation": "Recurse with a (min, max) bound: the current node's value must lie strictly between them. Going left tightens max to the node's value; going right tightens min. No extra list needed.",
          "code": "class Solution {\n    public boolean isValidBST(TreeNode root) {\n        return validate(root, null, null);\n    }\n\n    private boolean validate(TreeNode node, Long min, Long max) {\n        if (node == null) return true;\n        if (min != null && node.val <= min) return false;\n        if (max != null && node.val >= max) return false;\n        return validate(node.left, min, (long) node.val) && validate(node.right, (long) node.val, max);\n    }\n}",
          "output": "false",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(h) recursion stack"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-235",
      "number": 235,
      "title": "Lowest Common Ancestor of a Binary Search Tree",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-search-tree/",
      "problemStatement": "Given a BST and two nodes p and q, find their lowest common ancestor (the deepest node that has both p and q as descendants, a node can be a descendant of itself).",
      "examples": [
        { "input": "root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 8", "output": "6", "explanation": "2 is in the left subtree, 8 is in the right subtree of 6, so 6 is the split point." },
        { "input": "root = [6,2,8,0,4,7,9,null,null,3,5], p = 2, q = 4", "output": "2", "explanation": "4 is inside 2's own subtree, so 2 itself is the LCA." }
      ],
      "constraints": ["2 <= number of nodes <= 10^5", "all Node.val distinct", "p and q both exist in the tree"],
      "hints": ["Use the BST ordering: if both p and q are smaller than the current node, the LCA is in the left subtree; if both larger, it's in the right subtree; otherwise the current node is the split point."],
      "approaches": [
        {
          "label": "Optimal",
          "explanation": "Walk down from root: while both p.val and q.val are on the same side of the current node's value, move to that side. The first node where they diverge (or match) is the LCA.",
          "code": "class Solution {\n    public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) {\n        TreeNode node = root;\n        while (node != null) {\n            if (p.val < node.val && q.val < node.val) {\n                node = node.left;\n            } else if (p.val > node.val && q.val > node.val) {\n                node = node.right;\n            } else {\n                return node;\n            }\n        }\n        return null;\n    }\n}",
          "output": "6",
          "timeComplexity": "O(h)",
          "spaceComplexity": "O(1)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-450",
      "number": 450,
      "title": "Delete Node in a BST",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/delete-node-in-a-bst/",
      "problemStatement": "Given the root of a BST and a key, delete the node with that key (if it exists) and return the root of the resulting BST.",
      "examples": [
        { "input": "root = [5,3,6,2,4,null,7], key = 3", "output": "[4,2,6,null,null,null,7] (or [5,4,6,2,null,null,7], either valid BST shape accepted)", "explanation": "Node 3 has two children (2 and 4). Replace its value with the inorder successor (smallest node in right subtree = 4), then delete that successor node from the right subtree." }
      ],
      "constraints": ["0 <= number of nodes <= 10^4", "unique values"],
      "hints": ["Leaf node: just remove it (return null).", "One child: return that child, bypassing the deleted node.", "Two children: replace value with the inorder successor (min of right subtree), then recursively delete the successor from the right subtree."],
      "approaches": [
        {
          "label": "Optimal",
          "explanation": "Recursively locate the key using BST ordering. On finding it: no children -> null; one child -> return the existing child; two children -> find min of right subtree, copy its value up, then recursively delete that min value from the right subtree (which is now a one-or-zero-child deletion).",
          "code": "class Solution {\n    public TreeNode deleteNode(TreeNode root, int key) {\n        if (root == null) return null;\n        if (key < root.val) {\n            root.left = deleteNode(root.left, key);\n        } else if (key > root.val) {\n            root.right = deleteNode(root.right, key);\n        } else {\n            if (root.left == null) return root.right;\n            if (root.right == null) return root.left;\n            TreeNode successor = root.right;\n            while (successor.left != null) successor = successor.left;\n            root.val = successor.val;\n            root.right = deleteNode(root.right, successor.val);\n        }\n        return root;\n    }\n}",
          "output": "[4,2,6,null,null,null,7]",
          "timeComplexity": "O(h)",
          "spaceComplexity": "O(h)"
        }
      ]
    },
    {
      "type": "leetcode",
      "id": "lc-230",
      "number": 230,
      "title": "Kth Smallest Element in a BST",
      "difficulty": "Medium",
      "link": "https://leetcode.com/problems/kth-smallest-element-in-a-bst/",
      "problemStatement": "Given the root of a BST and an integer k, return the kth smallest value (1-indexed) among all node values.",
      "examples": [
        { "input": "root = [3,1,4,null,2], k = 1", "output": "1", "explanation": "Inorder traversal of a BST visits values in sorted order: 1, 2, 3, 4. The 1st smallest is 1." }
      ],
      "constraints": ["1 <= number of nodes <= 10^4", "0 <= Node.val <= 10^4"],
      "hints": ["Inorder traversal of a BST produces values in ascending order.", "Stop as soon as you've visited the kth value -- no need to traverse the whole tree."],
      "approaches": [
        {
          "label": "Brute Force",
          "explanation": "Do a full inorder traversal collecting every value into a list, then index into position k-1. Wastes time/space traversing past the kth node.",
          "code": "class Solution {\n    public int kthSmallest(TreeNode root, int k) {\n        List<Integer> values = new ArrayList<>();\n        inorder(root, values);\n        return values.get(k - 1);\n    }\n\n    private void inorder(TreeNode node, List<Integer> values) {\n        if (node == null) return;\n        inorder(node.left, values);\n        values.add(node.val);\n        inorder(node.right, values);\n    }\n}",
          "output": "1",
          "timeComplexity": "O(n)",
          "spaceComplexity": "O(n)"
        },
        {
          "label": "Optimal",
          "explanation": "Iterative inorder traversal using an explicit stack, decrementing a counter k as each node is visited and returning as soon as k hits 0 -- avoids building the full list and can stop early.",
          "code": "class Solution {\n    public int kthSmallest(TreeNode root, int k) {\n        Deque<TreeNode> stack = new ArrayDeque<>();\n        TreeNode node = root;\n        while (node != null || !stack.isEmpty()) {\n            while (node != null) {\n                stack.push(node);\n                node = node.left;\n            }\n            node = stack.pop();\n            k--;\n            if (k == 0) return node.val;\n            node = node.right;\n        }\n        return -1;\n    }\n}",
          "output": "1",
          "timeComplexity": "O(h + k)",
          "spaceComplexity": "O(h)"
        }
      ]
    }
  ],
  "codeExamples": [
    { "id": "java-bst-insert", "lang": "java", "filename": "Main.java", "code": "public class Main {\n    static class Node {\n        int data;\n        Node left, right;\n        Node(int data) { this.data = data; }\n    }\n\n    static Node insert(Node node, int value) {\n        if (node == null) {\n            return new Node(value); // base case -- found the empty spot\n        }\n        if (value < node.data) {\n            node.left = insert(node.left, value);\n        } else if (value > node.data) {\n            node.right = insert(node.right, value);\n        }\n        return node;\n    }\n\n    static void printInOrder(Node node) {\n        if (node == null) return;\n        printInOrder(node.left);\n        System.out.print(node.data + \" \");\n        printInOrder(node.right);\n    }\n\n    public static void main(String[] args) {\n        Node root = null;\n        int[] values = {50, 30, 70, 20, 40, 60, 80};\n        for (int v : values) {\n            root = insert(root, v);\n        }\n        printInOrder(root);\n    }\n}", "output": "20 30 40 50 60 70 80 " },
    { "id": "java-bst-search", "lang": "java", "filename": "Main.java", "code": "public class Main {\n    static class Node {\n        int data;\n        Node left, right;\n        Node(int data) { this.data = data; }\n    }\n\n    static Node insert(Node node, int value) {\n        if (node == null) return new Node(value);\n        if (value < node.data) node.left = insert(node.left, value);\n        else if (value > node.data) node.right = insert(node.right, value);\n        return node;\n    }\n\n    static boolean search(Node node, int value) {\n        if (node == null) {\n            return false; // reached an empty spot -- not found\n        }\n        if (value == node.data) {\n            return true;\n        }\n        return value < node.data ? search(node.left, value) : search(node.right, value);\n    }\n\n    public static void main(String[] args) {\n        Node root = null;\n        int[] values = {50, 30, 70, 20, 40, 60, 80};\n        for (int v : values) {\n            root = insert(root, v);\n        }\n\n        System.out.println(search(root, 60));\n        System.out.println(search(root, 100));\n    }\n}", "output": "true\nfalse" }
  ],
  "retypePractice": [
    { "id": "java-bst-insert", "reference": "public class Main {\n    static class Node {\n        int data;\n        Node left, right;\n        Node(int data) { this.data = data; }\n    }\n\n    static Node insert(Node node, int value) {\n        if (node == null) {\n            return new Node(value); // base case -- found the empty spot\n        }\n        if (value < node.data) {\n            node.left = insert(node.left, value);\n        } else if (value > node.data) {\n            node.right = insert(node.right, value);\n        }\n        return node;\n    }\n\n    static void printInOrder(Node node) {\n        if (node == null) return;\n        printInOrder(node.left);\n        System.out.print(node.data + \" \");\n        printInOrder(node.right);\n    }\n\n    public static void main(String[] args) {\n        Node root = null;\n        int[] values = {50, 30, 70, 20, 40, 60, 80};\n        for (int v : values) {\n            root = insert(root, v);\n        }\n        printInOrder(root);\n    }\n}" },
    { "id": "java-bst-search", "reference": "public class Main {\n    static class Node {\n        int data;\n        Node left, right;\n        Node(int data) { this.data = data; }\n    }\n\n    static Node insert(Node node, int value) {\n        if (node == null) return new Node(value);\n        if (value < node.data) node.left = insert(node.left, value);\n        else if (value > node.data) node.right = insert(node.right, value);\n        return node;\n    }\n\n    static boolean search(Node node, int value) {\n        if (node == null) {\n            return false; // reached an empty spot -- not found\n        }\n        if (value == node.data) {\n            return true;\n        }\n        return value < node.data ? search(node.left, value) : search(node.right, value);\n    }\n\n    public static void main(String[] args) {\n        Node root = null;\n        int[] values = {50, 30, 70, 20, 40, 60, 80};\n        for (int v : values) {\n            root = insert(root, v);\n        }\n\n        System.out.println(search(root, 60));\n        System.out.println(search(root, 100));\n    }\n}" }
  ],
  "practiceQuestions": [],
  "interviewQuestions": true,
  "leetcode": [],
  "completionRules": {
    "requireAllSectionsViewed": true,
    "requireRetypeIds": ["java-bst-insert", "java-bst-search"]
  }
}
